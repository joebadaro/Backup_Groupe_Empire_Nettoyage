---
interface Props {
  videoId: string;
  title: string;
  posterUrl?: string; // Opt-in custom high-res poster
  aspectRatio?: string;
  class?: string;
}

const {
  videoId,
  title,
  posterUrl,
  aspectRatio = "16/9",
  class: className = "",
} = Astro.props;

// For production, using a custom WebP poster is highly recommended for best quality,
// but for Shorts and default videos, hqdefault.jpg natively guarantees existence.
const fallbackPoster = `https://i.ytimg.com/vi/${videoId}/hqdefault.jpg`;
const finalPoster = posterUrl || fallbackPoster;
---

<div 
  class:list={["yt-facade", className]} 
  data-yid={videoId} 
  style={`--aspect: ${aspectRatio};`}
  role="button"
  tabindex="0"
  aria-label={`Lancer la vidéo : ${title}`}
>
  <img 
    src={finalPoster} 
    alt={`Aperçu de la vidéo : ${title}`} 
    loading="lazy" 
    class="yt-facade-poster"
  />
  <div class="yt-play-overlay">
    <button class="yt-play-btn" aria-hidden="true" tabindex="-1">
      <svg viewBox="0 0 24 24" fill="currentColor" stroke="none">
        <path d="M8 5v14l11-7z" />
      </svg>
    </button>
  </div>
</div>

<style>
  .yt-facade {
    position: relative;
    width: auto;
    max-width: 100%;
    max-height: 75vh;
    margin: 0 auto; /* Ensure it stays centered if scaling down */
    aspect-ratio: var(--aspect);
    background-color: #080d14; /* Dark theme navy */
    border-radius: 12px;
    overflow: hidden;
    cursor: pointer;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(255,255,255,0.1);
  }

  .yt-facade:hover, .yt-facade:focus-visible {
    transform: translateY(-4px);
    box-shadow: 0 16px 35px rgba(0, 31, 63, 0.25);
    outline: none;
  }

  .yt-facade-poster {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.85;
    transition: opacity 0.4s ease, transform 0.4s ease;
  }

  .yt-facade.is-loaded .yt-facade-poster,
  .yt-facade.is-loaded .yt-play-overlay {
    display: none;
  }

  /* Deepen the image when hovering for cinematic effect */
  .yt-facade:hover .yt-facade-poster {
    opacity: 0.75;
    transform: scale(1.02);
  }

  .yt-play-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
  }

  /* Glassmorphism premium play button */
  .yt-play-btn {
    position: relative;
    width: 72px;
    height: 72px;
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.5);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
  }

  .yt-play-btn svg {
    width: 34px;
    height: 34px;
    margin-left: 4px; /* Optically center the play triangle */
    filter: drop-shadow(0 2px 4px rgba(0,0,0,0.3));
    transition: color 0.3s ease;
  }

  /* Elegant pulsing aura */
  .yt-play-btn::before {
    content: '';
    position: absolute;
    inset: -15px;
    border-radius: 50%;
    border: 1.5px solid rgba(255, 255, 255, 0.4);
    animation: play-pulse 2.5s infinite cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0;
    pointer-events: none;
  }

  @keyframes play-pulse {
    0% { transform: scale(0.85); opacity: 0; border-width: 2px; }
    50% { opacity: 0.6; }
    100% { transform: scale(1.3); opacity: 0; border-width: 0.5px; }
  }

  /* On hover, transition directly to the brand's premium Gold (#f0c14b) */
  .yt-facade:hover .yt-play-btn, .yt-facade:focus-visible .yt-play-btn {
    background: rgba(240, 193, 75, 0.95);
    border-color: #f0c14b;
    transform: scale(1.08);
    box-shadow: 0 8px 25px rgba(240, 193, 75, 0.4);
  }

  .yt-facade:hover .yt-play-btn svg, .yt-facade:focus-visible .yt-play-btn svg {
    color: #001f3f; /* Deep brand blue for contrast against gold */
    filter: none;
  }

  .yt-facade:hover .yt-play-btn::before {
    border-color: rgba(240, 193, 75, 0.6);
  }

  /* Support original inline desktop video layer natively */
  :global(.yt-facade iframe) {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
    background: #000;
  }

  /* Note: Full-width structural expansion on Mobile is securely handled directly via JS ancestor manipulation */
</style>

<script>
  // Ghost Overlay State Architecture avoiding Safari strict autoplay penalties
  let ytGhostPlayer: any = null;
  let isYtApiReady = false;
  let ghostSyncInterval: any = null;

  const initGhostOverlay = () => {
    if (document.getElementById('yt-ghost-wrapper')) return;
    
    const wrapper = document.createElement('div');
    wrapper.id = 'yt-ghost-wrapper';
    wrapper.style.position = 'absolute';
    wrapper.style.zIndex = '999';
    wrapper.style.display = 'none'; // Hidden structurally until tap triggers API bypass
    wrapper.style.border = 'none';
    wrapper.style.backgroundColor = '#000';

    const iframeBox = document.createElement('div');
    iframeBox.id = 'yt-ghost-iframe';

    wrapper.appendChild(iframeBox);
    document.body.appendChild(wrapper);
  };

  const syncGhostPosition = (facade: HTMLElement) => {
    const wrapper = document.getElementById('yt-ghost-wrapper');
    if (!wrapper) return;
    // Map absolute coords aggressively matching the active card position safely natively
    const rect = facade.getBoundingClientRect();
    wrapper.style.top = `${rect.top + window.scrollY}px`;
    wrapper.style.left = `${rect.left + window.scrollX}px`;
    wrapper.style.width = `${rect.width}px`;
    wrapper.style.height = `${rect.height}px`;
  };

  const startGhostSync = (facade: HTMLElement) => {
    if (ghostSyncInterval) clearInterval(ghostSyncInterval);
    syncGhostPosition(facade);
    ghostSyncInterval = setInterval(() => {
      syncGhostPosition(facade);
    }, 50); // Polling sufficiently fast to remain visuo-locked safely against UI jumps/reflows
  };

  const stopGhostSync = () => {
    if (ghostSyncInterval) clearInterval(ghostSyncInterval);
    const wrapper = document.getElementById('yt-ghost-wrapper');
    if (wrapper) {
      wrapper.style.display = 'none';
      wrapper.style.top = '-9999px';
    }
  };

  if (typeof window !== 'undefined') {
    (window as any).onYouTubeIframeAPIReady = () => {
      isYtApiReady = true;
      if (document.getElementById('yt-ghost-iframe')) {
        ytGhostPlayer = new (window as any).YT.Player('yt-ghost-iframe', {
          height: '100%',
          width: '100%',
          playerVars: {
            playsinline: 1,
            rel: 0,
            showinfo: 0,
            modestbranding: 1,
            autoplay: 1,
            controls: 1
          }
        });
      }
    };
  }

  const loadYTAPI = () => {
    if (document.getElementById('youtube-api-script')) return;
    initGhostOverlay();
    const tag = document.createElement('script');
    tag.id = 'youtube-api-script';
    tag.src = 'https://www.youtube.com/iframe_api';
    document.head.appendChild(tag);
  };

  const loadYoutubeGhost = (facade: HTMLElement, videoId: string) => {
    if (facade.classList.contains('is-loaded')) return;
    
    // Instantly mute and clear the API layer from previously active cards
    if (ytGhostPlayer && typeof ytGhostPlayer.stopVideo === 'function') {
      ytGhostPlayer.stopVideo();
    }
    stopGhostSync();

    // Structurally reset all old facade grid markers naturally
    const activeFacades = document.querySelectorAll('.yt-facade.is-loaded');
    activeFacades.forEach((activeNode) => {
      if (activeNode !== facade) {
        activeNode.classList.remove('is-loaded');
        const oldParentCard = activeNode.closest('.video-card') as HTMLElement;
        if (oldParentCard) oldParentCard.style.gridColumn = '';
      }
    });

    facade.classList.add('is-loaded');

    // EXPLICIT STRUCTURAL EXPANSION: Let grid natively expand before parsing coords
    const parentCard = facade.closest('.video-card') as HTMLElement;
    if (parentCard && window.innerWidth <= 768) {
      parentCard.style.gridColumn = '1 / -1';
    }

    // Push execution cleanly enforcing structural layout reflow completion natively 
    setTimeout(() => {
      const wrapper = document.getElementById('yt-ghost-wrapper');
      if (wrapper) {
        wrapper.style.display = 'block';
        startGhostSync(facade);
      }

      // Bypass playback restrictions reliably by executing direct singleton telemetry binding!
      if (isYtApiReady && ytGhostPlayer && typeof ytGhostPlayer.loadVideoById === 'function') {
        ytGhostPlayer.loadVideoById(videoId);
      } else {
        // Safely failback to basic parameter injections relying strictly against DOM
        const fallback = document.getElementById('yt-ghost-iframe') as HTMLIFrameElement;
        if (fallback) {
          fallback.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&playsinline=1&rel=0&showinfo=0&modestbranding=1`;
        }
      }

      // Trigger final cinematic smooth auto-centering on Mobile
      if (window.innerWidth <= 768 && parentCard) {
        parentCard.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    }, 60);
  };

  const initFacades = () => {
    // Explicit API Pre-warm immediately on module cycle startup universally
    setTimeout(loadYTAPI, 750);

    const facades = document.querySelectorAll('.yt-facade');
    facades.forEach(facade => {
      const clone = facade.cloneNode(true) as HTMLElement;
      if (facade.parentNode) facade.parentNode.replaceChild(clone, facade);

      clone.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          const videoId = clone.getAttribute('data-yid');
          if (videoId) loadYoutubeGhost(clone as HTMLElement, videoId);
        }
      });
      
      clone.addEventListener('click', () => {
        const videoId = clone.getAttribute('data-yid');
        if (videoId) loadYoutubeGhost(clone as HTMLElement, videoId);
      });
    });
  };

  // Run on first load and respect Astro page transitions flawlessly universally
  document.addEventListener('DOMContentLoaded', initFacades);
  document.addEventListener('astro:page-load', initFacades);
</script>
